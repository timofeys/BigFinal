                           
Процедура ПроверитьПользователя() Экспорт
	#Если Клиент Тогда
			ТекПользователь = ПользователиИнформационнойБазы.ТекущийПользователь();

		Если НЕ ЗначениеЗаполнено(ПараметрыСеанса.Пользователь)И НЕ ТекПользователь.Имя ="Админ" Тогда
			Предупреждение("Работа неидентифицированных пользователей в базе не допускается.");
			ЗавершитьРаботуСистемы(Ложь);
			Иначе
		ТекЗаголовок = Метаданные.Синоним;
		УстановитьЗаголовокСистемы(ТекЗаголовок+" - "+ПараметрыСеанса.Пользователь); 
		КонецЕсли;
	#КонецЕсли
КонецПроцедуры
	
